"""
Pytest fixtures for backend tests.

Provides:
    - Async test client (httpx.AsyncClient)
    - Test database session (SQLite or PostgreSQL testcontainers)
    - Factory fixtures for models (Company, User, PO, etc.)
    - Auth token fixtures (mock Keycloak JWTs)
"""

# TODO: Sprint 1 — Implement test fixtures
# import pytest
# from httpx import AsyncClient
